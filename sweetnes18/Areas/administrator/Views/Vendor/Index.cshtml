@model IEnumerable<sweetnes18.Areas.administrator.Models.user>

@{
    ViewBag.Title = "Products List";
    Layout = "~/Areas/administrator/Views/Shared/_Layout.cshtml";
}

<div class="container" ng-controller="ShowAllVendorList" ng-init="ShowAllVendor()">
    
    
    @*<div class="col-md-12 ">
            <div class="btn-group btn-light" role="group" aria-label="Basic example">
                <button type="button" class="btn btn-secondary">Available</button>
                <button type="button" class="btn btn-secondary">Pending</button>
                <button type="button" class="btn btn-secondary">Addintional fees</button>
                <button type="button" class="btn btn-secondary">Now filter</button>
            </div>
        </div>*@
    <div class="col-md-6">
        <div class="dataTables_length">

        </div>
    </div>
    <table class="table table-striped">
        @*// table-responsive*@
     <thead>
         <tr>
             <th>Name</th>
             <th>Mobile</th>
             

             <th>Total Income</th>
             <th>Paid </th>
             
             <th>Cancel</th>
             <th>pending</th>
             <th>Unpaid</th>
             <th>Action</th>
         </tr>
        </thead>
        <tbody>
            <tr ng-repeat="i in vendor">
                <td>{{i.user.fname}} {{i.user.lname}}</td>
                <td>{{i.user.mobileno}}</td>
                
                <td>{{i.TotalAmount|number :2}} AED</td>
                <td style="background:#cddba6; color:#000;">{{i.PaidAmount|number :2}} AED</td>
                <td style="background:red; color:#fff">{{i.CancleAmount|number :2}} AED</td>
                
                <td style="background:#b6ff00; color:#000;">{{i.PandingAmount|number :2}} AED</td>
                <td style="background:#40384d; color:#fff;">{{i.unpaidAmount|number :2}} AED</td>
                <td><a href="/administrator/Vendor/Details/{{i.user.id}}"><i class="btn btn-info  fa fa-align-justify"></i></a><a href="/administrator/Vendor/Edit/{{i.user.id}}"><i class="btn btn-info fa fa fa-dollar"></i></a></td>
            </tr>
        </tbody>
    </table>
</div>